import {createContext} from "react";
//createContext creates customHooks for me to use.
const GithubContext = createContext(); //' simply by using this GitHubContext has been created.

export default GithubContext;